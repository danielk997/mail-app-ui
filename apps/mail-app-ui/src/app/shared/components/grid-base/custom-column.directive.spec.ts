import { CustomColumnDirective } from './custom-column.directive';

describe('CustomColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomColumnDirective();
    expect(directive).toBeTruthy();
  });
});
